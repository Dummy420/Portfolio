import { ProgramsDirective } from './programs.directive';

describe('ProgramsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgramsDirective();
    expect(directive).toBeTruthy();
  });
});
