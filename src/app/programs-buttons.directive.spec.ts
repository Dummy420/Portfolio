import { ProgramsButtonsDirective } from './programs-buttons.directive';

describe('ProgramsButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgramsButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
